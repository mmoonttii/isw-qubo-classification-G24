Now I'll create the generator script (to produce the static file once, reproducibly), generate the actual CSV, and rewrite the fixture to consume it statically instead of generating data at test time.Good, the static file is generated correctly (120 rows + header). Now I'll rewrite the fixture section of `test_pipeline.py` to consume this file statically instead of generating it in-memory.Now the core change — replace the `session_dir` + `raw_csv` generation fixtures with a static-file loader:Good — syntax is valid, `numpy` is still used elsewhere, and `session_dir` is fully gone. Now let's do a full sanity pass on the file and view the top section to confirm it reads cleanly.This looks correct. Now let's actually try to run the test file against a minimal stub package to catch any real errors (import errors, fixture wiring issues) rather than just syntax-checking.The fixture logic checks out against the static file (pytest itself isn't installable in this sandbox — no network — but I validated the logic manually and confirmed the rest of the file is syntactically valid and all downstream references to `raw_csv`/constants are unaffected).What changed, in short:

- **`raw_csv` fixture** no longer generates data in memory/tmp dirs. It now just loads `data/sample_test_dataset.csv`, fails with a clear message if the file is missing, and sanity-checks the §13 "≥10% per class" invariant.
- **`session_dir` fixture** removed (it was only used by the old generator).
- **`scripts/generate_sample_dataset.py`** — new one-time script that builds the static file (same shape/logic as the old fixture, fixed seed). Run it once, commit the resulting CSV; tests never call it.
- **`data/sample_test_dataset.csv`** — the actual static file, generated and included here, ready to drop into your repo.

Place all three where they belong (`tests/test_pipeline.py`, `scripts/generate_sample_dataset.py`, `data/sample_test_dataset.csv`) and `pytest` will run entirely against the fixed file — no dataset is created or overwritten during test runs anymore.