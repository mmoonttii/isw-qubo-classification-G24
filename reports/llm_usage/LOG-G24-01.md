# LLM Interaction Log 

---

## Metadata

```yaml
group_id: "G24"
repository_url: "https://github.com/mmoonttii/isw-qubo-classification-G24/"
students:
  - matricola: "60/61/66424"
    name: "Lorenzo Montalto"
  - matricola: "60/61/66409"
    name: "Chiara Didu"
last_update: "2026-06-23"
log_id: "G24-01"
```

---

# Interaction Entries

---

## Interaction G24-06-23-001
```yaml
llm_name: "Gemini"
llm_version_or_model: "Gemini 3.1 Pro"
chat_name_or_identifier: "Software Engineering Project Roadmap"
interaction_mode: "web_chat"
timestamp: "16:07" 
```

### 2. Author of the interaction

```yaml
performed_by: "couple"
```

### 3. Project phase

```yaml
project_phase:
  - "requirements_understanding"
```

### 4. Input files and/or context provided to the LLM

```yaml
input_context:
  files_uploaded:
    - file_name: specifiche_progetto.pdf
      repository_path: reports/llm_usage/files/specifiche_progetto.pdf
      description: Project specifications
  code_snippets: null
  error_messages: null
  dataset_excerpts: null
  prompt_generated_by: null
```

### 5. Student prompt

```text
These are the specs for a Software Engineering project I'm working on for uni.
It has to be done mainly using AIs, in my case the free version of Claude. After you analyzed these specs, suggest a roadmap for the project and the first prompt to send to Claude Sonnet. Conversations with chatbots should strictly be in English.
```

### 6. LLM response

If the full response is stored in a separate file:
```yaml
response_stored_in_file:
  used: true
  file_name: LLM_G24-06-23-001_response.md
  repository_path: reports/llm_usage/files/llm_responses/G24-01/LLM_G24-06-23-001_response.md
```

### 7. Files generated or downloaded from the LLM response

```yaml
generated_or_downloaded_files:
  - unique_file_name: null
    repository_path: null
    file_type: null       # e.g. py, ipynb, md, yaml, csv, json, txt
    created_from_response: false
    short_description: null
    referenced_in_response: false
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: false  # true / false
  modified_before_use: true  # true / false
  description_of_modifications: we changed "We must create a specific folder structure" in "This is the repository structure" in the prompt for Claude because we had already created it and we added that it should be able to handle dataset of more than 1.5 million records
  related_repository_files:
    - null
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: no  # "yes"/"no"  The subsequent part only if "yes"
  issue_categories:    # Delete non-pertinent ones
    - "none"
  description: null
  how_issue_was_resolved: null
```

### 10. Usefulness and reliability assessment

Use a scale from 1 to 5 (1 = lowest grade, 5 = highest grade).

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 4
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: true
  notes: "On the 23rd we couldn't do any interactions with Claude because it was down"
```

---

## Interaction G24-06-24-002

### 1. LLM and chat information

```yaml
llm_name: "Claude"
llm_version_or_model: "Claude Sonnet 4.6 High"
chat_name_or_identifier: "Binary classification QUBO feature reduction preprocessing module"
interaction_mode: "web_chat"
timestamp: 11:00

```

### 2. Author of the interaction

```yaml
performed_by: "couple"
```

### 3. Project phase

```yaml
project_phase:
  - "preprocessing"
```

### 4. Input files and/or context provided to the LLM

List any files or project documents provided to the LLM.
Record ("yes"/"no") if you provided code snippets, error messages, dataset excerpts.
If the prompt was itself generated or improved by another LLM interaction, mention the source interaction ID.

```yaml
input_context:
  files_uploaded:
    - file_name: specifiche_progetto.pdf
      repository_path: reports/llm_usage/files/specifiche_progetto.pdf
      description: Project Specifications
  code_snippets: no
  error_messages: no
  dataset_excerpts: no
  prompt_generated_by: G24-06-23-001 
```

### 5. Student prompt

```text
Act as an expert Software Engineer and Python Data Scientist. I am working on a university project about "Binary Classification with QUBO Feature Reduction" using Python 3.11+. We must communicate STRICTLY in English. 

Here is the context and the strict constraints for the project:
1. This is the repository structure:
isw-qubo-classification-GXX/
├── README.md
├── requirements.txt
├── data/
├── src/
│   └── qubo_project/
│       ├── __init__.py
│       ├── preprocessing.py
│       ├── feature_selection.py
│       ├── model.py
│       └── gui.py
├── tests/
└── outputs/

2. I need your help to write the first module: `src/qubo_project/preprocessing.py`. 
It must contain a specific function signature:
\```python
def fit_normalize(
    input_csv: str, 
    target_column: str, 
    normalized_csv: str, 
    outInitalRes_json: str, 
    minPercValid: float = 0.05
):
\```

This function must:
- Read a CSV dataset (handling missing values).
- Separate the target column (which contains binary 0/1 values).
- Drop features (columns) that have less than `minPercValid` percentage of valid, non-zero data (i.e., drop columns that are mostly zeros or NaNs).
- Apply Z-score normalization to the remaining features.
- Save the resulting normalized dataset to `normalized_csv` keeping the original headers and the target column.
- Save a JSON file to `outInitalRes_json` containing exactly these keys: "n_input_features", "n_kept_features", "dataset_size", "dataset_input_time", "dataset_processing_time", and "dropped_feature_names".

3. The file `preprocessing.py` must also be executable from the command line using `argparse` with this exact syntax:
```
```bash
python preprocessing.py \
  --input dati_credito.csv \
  --target target \
  --out-data normalized.csv \
  --out-json preprocessing_result.json \
  --min-perc-valid 0.06
```
```
Beware that the implementation should be able to handle datasets of more than 1.5 million records

Please provide:
1. An initial list of libraries I should put in `requirements.txt` for this data processing step (e.g., pandas, scikit-learn).
2. The complete, production-ready code for `src/qubo_project/preprocessing.py` fulfilling all the requirements, including docstrings and the CLI implementation. Ensure reproducibility and no absolute paths.
```


### 6. LLM response

```text
```

If the full response is stored in a separate file:

```yaml
response_stored_in_file:
  used: true
  file_name: LLM_G24-06-24-002_response.md
  repository_path: reports/llm_usage/files/llm_responses/G24-01/LLM_G24-06-24-002_response.md
```

### 7. Files generated or downloaded from the LLM response

List every file produced by the LLM and downloaded or copied into the project. Each file name must be unique across the whole repository.

```yaml
generated_or_downloaded_files:
  - unique_file_name: LLM_G24-06-24-002_preprocessing.py
    repository_path: reports/llm_usage/files/generated_files/G24-01/LLM_G24-06-24-002_preprocessing.py
    file_type: py       # e.g. py, ipynb, md, yaml, csv, json, txt
    created_from_response: true
    short_description: Preprocessing
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true  # true / false
  modified_before_use: false   # true / false
  description_of_modifications: null
  related_repository_files:
    - null
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: no    # "yes"/"no"  The subsequent part only if "yes"
  issue_categories:    # Delete non-pertinent ones
    - "none"
  description: null
  how_issue_was_resolved: null
```

### 10. Usefulness and reliability assessment

Use a scale from 1 to 5 (1 = lowest grade, 5 = highest grade).

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 4
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: yes
  notes: null
```

---

## Interaction G24-06-25-003

### 1. LLM and chat information

```yaml
llm_name: "Claude"
llm_version_or_model: "Claude Sonnet 4.6"
chat_name_or_identifier: "Short name or identifier of the active chat"
interaction_mode: "web_chat"
timestamp: #TODO
```

### 2. Author of the interaction

```yaml
performed_by: "couple"   # use "couple" or the matricola of the individual student
```

### 3. Project phase

Select one or more phases.

```yaml
project_phase:
  - "preprocessing"
```

### 4. Input files and/or context provided to the LLM

List any files or project documents provided to the LLM.
Record ("yes"/"no") if you provided code snippets, error messages, dataset excerpts.
If the prompt was itself generated or improved by another LLM interaction, mention the source interaction ID.

```yaml
input_context:
  files_uploaded:
    - file_name: specifiche_progetto.pdf
      repository_path: reports/llm_usage/files/specifiche_progetto.pdf
      description: Project Specifications
    - file_name: preprocessing.py
      repository_path: src/qubo_project/preprocessing.py
  code_snippets: null
  error_messages: null
  dataset_excerpts: null
  prompt_generated_by: null 
```

### 5. Student prompt

Paste the exact prompt sent to the LLM. 

```text
Check if this code is compliant with the project specs attached for the preprocesing.py file
```

### 6. LLM response

Paste the full response received from the LLM. If the response is very long, it is acceptable to store it in a separate file and reference it here.

```text
```

If the full response is stored in a separate file:

```yaml
response_stored_in_file:
  used: true
  file_name: LLM_G24-06-25-003_response.md
  repository_path: reports/llm_usage/files/llm_responses/G24-01/LLM_G24-06-25-003_response.md
```

### 7. Files generated or downloaded from the LLM response

List every file produced by the LLM and downloaded or copied into the project. Each file name must be unique across the whole repository.

```yaml
generated_or_downloaded_files:
  - unique_file_name: null
    repository_path: null
    file_type: null       # e.g. py, ipynb, md, yaml, csv, json, txt
    created_from_response: true
    short_description: null
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: false  # true / false
  modified_before_use: true   # true / false
  description_of_modifications: Most of the answer is correct, although we noticed that Claude considered an error the fact that the dataset splitting happens in another component 
  related_repository_files:
    - null
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: no    # "yes"/"no"  The subsequent part only if "yes"
  issue_categories:    # Delete non-pertinent ones
    - "none"
  description: null
  how_issue_was_resolved: null
```

### 10. Usefulness and reliability assessment

Use a scale from 1 to 5 (1 = lowest grade, 5 = highest grade).

```yaml
assessment:
  usefulness_1_to_5: null
  correctness_1_to_5: null
  clarity_1_to_5: null
  confidence_after_verification_1_to_5: null
  would_reuse_this_output: null
  notes: null
```

---

## Interaction G24-06-25-004

### 1. LLM and chat information

```yaml
llm_name: "Claude"
llm_version_or_model: "Claude Sonnet 4.6"
chat_name_or_identifier: "Code compliance check for `preprocessing.py`"
interaction_mode: "web_chat"
timestamp: #TODO
```

### 2. Author of the interaction

```yaml
performed_by: "couple"   # use "couple" or the matricola of the individual student
```

### 3. Project phase

Select one or more phases.

```yaml
project_phase:
  - "preprocessing"
```

### 4. Input files and/or context provided to the LLM

List any files or project documents provided to the LLM.
Record ("yes"/"no") if you provided code snippets, error messages, dataset excerpts.
If the prompt was itself generated or improved by another LLM interaction, mention the source interaction ID.

```yaml
input_context:
  files_uploaded:
    - file_name: null
      repository_path: null
      description: null
  code_snippets: null
  error_messages: null
  dataset_excerpts: null
  prompt_generated_by: null 
```

### 5. Student prompt

Paste the exact prompt sent to the LLM. 

```text
Generate the prompt for the chat with Cloude Sonnet 4.6 where we generated the preprocesing.py code to add the target binary validation
```

### 6. LLM response

Paste the full response received from the LLM. If the response is very long, it is acceptable to store it in a separate file and reference it here.

```text
Here's a focused prompt you can send directly to the Claude API (or paste into a chat) to add the target binary validation:

---

The preprocessing.py code we generated is mostly compliant with the project specs,
but it is missing one validation: the target column is cast to int but never checked
to confirm it actually contains only binary values (0 and 1).

Please add a validation pass between the end of Pass 1 and the column filtering
section. Requirements:

- Read the target column in chunks using the existing _CHUNK_SIZE and chunked 
  reading pattern, consistent with how Pass 1 and Pass 2 are structured.
- Collect unique values across all chunks.
- Raise a ValueError with a descriptive message that includes the unexpected 
  values found if the unique values are not a subset of {0, 1}.
- Log an INFO message using the existing logger confirming validation passed,
  showing the unique values (e.g. {0, 1}).
- Use a comment header that matches the existing style (like # PASS 1, # PASS 2).
- No new imports or dependencies.
- Do not change the function signature or any existing logic.

Return only the new block to insert, not the full file.
```

If the full response is stored in a separate file:

```yaml
response_stored_in_file:
  used: false
  file_name: null
  repository_path: null
```

### 7. Files generated or downloaded from the LLM response

List every file produced by the LLM and downloaded or copied into the project. Each file name must be unique across the whole repository.

```yaml
generated_or_downloaded_files:
  - unique_file_name: null
    repository_path: null
    file_type: null       # e.g. py, ipynb, md, yaml, csv, json, txt
    created_from_response: true
    short_description: null
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true  # true / false
  modified_before_use: false   # true / false
  description_of_modifications: null
  related_repository_files:
    - null
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: null    # "yes"/"no"  The subsequent part only if "yes"
  issue_categories:    # Delete non-pertinent ones
    - "none"
  description: null
  how_issue_was_resolved: null
```

### 10. Usefulness and reliability assessment

Use a scale from 1 to 5 (1 = lowest grade, 5 = highest grade).

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 4
  clarity_1_to_5: 4
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: true
  notes: null
```

---

## Interaction G24-06-25-005

### 1. LLM and chat information

```yaml
llm_name: "Claude"
llm_version_or_model: "Claude Sonnet 4.6"
chat_name_or_identifier: "Binary classification QUBO feature reduction preprocessing module"
interaction_mode: "web_chat"
timestamp: #TODO
```

### 2. Author of the interaction

```yaml
performed_by: "couple"   # use "couple" or the matricola of the individual student
```

### 3. Project phase

Select one or more phases.

```yaml
project_phase:
  - "preprocessing"
```

### 4. Input files and/or context provided to the LLM

List any files or project documents provided to the LLM.
Record ("yes"/"no") if you provided code snippets, error messages, dataset excerpts.
If the prompt was itself generated or improved by another LLM interaction, mention the source interaction ID.

```yaml
input_context:
  files_uploaded:
    - file_name: null
      repository_path: null
      description: null
  code_snippets: null
  error_messages: null
  dataset_excerpts: null
  prompt_generated_by: G24-06-25-004 
```

### 5. Student prompt

Paste the exact prompt sent to the LLM. 

```text
The preprocessing.py code you generated is mostly compliant with the project specs, but it is missing one validation: the target column is cast to int but never checked to confirm it actually contains only binary values (0 and 1).

Please add a validation pass between the end of Pass 1 and the column filtering section.

Requirements:

- Read the target column in chunks using the existing _CHUNK_SIZE and chunked reading pattern, consistent with how Pass 1 and Pass 2 are structured.
- Collect unique values across all chunks.
- Raise a ValueError with a descriptive message that includes the unexpected 
  values found if the unique values are not a subset of {0, 1}.
- Log an INFO message using the existing logger confirming validation passed, showing the unique values (e.g. {0, 1}).
- Use a comment header that matches the existing style (like # PASS 1, # PASS 2).
- No new imports or dependencies.
- Do not change the function signature or any existing logic.

Return only the new block to insert, not the full file.
```

### 6. LLM response

Paste the full response received from the LLM. If the response is very long, it is acceptable to store it in a separate file and reference it here.

```text
```

If the full response is stored in a separate file:

```yaml
response_stored_in_file:
  used: true
  file_name: LLM_G24-06-25-005_response.md
  repository_path: reports/llm_usage/files/llm_responses/G24-01/LLM_G24-06-25-005_response.md
```

### 7. Files generated or downloaded from the LLM response

List every file produced by the LLM and downloaded or copied into the project. Each file name must be unique across the whole repository.

```yaml
generated_or_downloaded_files:
  - unique_file_name: null
    repository_path: null
    file_type: null       # e.g. py, ipynb, md, yaml, csv, json, txt
    created_from_response: true
    short_description: null
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true  # true / false
  modified_before_use: false   # true / false
  description_of_modifications: null
  related_repository_files:
    - null
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: no    # "yes"/"no"  The subsequent part only if "yes"
  issue_categories:    # Delete non-pertinent ones
    - "none"
  description: null
  how_issue_was_resolved: null
```

### 10. Usefulness and reliability assessment

Use a scale from 1 to 5 (1 = lowest grade, 5 = highest grade).

```yaml
assessment:
  usefulness_1_to_5: 4
  correctness_1_to_5: 4
  clarity_1_to_5: 4
  confidence_after_verification_1_to_5: 4
  would_reuse_this_output: yes
  notes: null
```
---

# Suggested file naming convention

Use unique and descriptive names for every file generated or downloaded from an LLM response.

Recommended format:

```text
LLM_<interaction_id>_<short_description>.<extension>
```

Examples:

```text
LLM_G03-06-05-002_preprocessing_function.py
LLM_G03-06-12-001_qubo_cost_function.py
LLM_G03-06-12-001_test_feature_selection.py
LLM_G03-06-13-003_readme_section.md
```

---

# Final checklist

Before submission, verify that:

- [ ] Every relevant LLM interaction has been recorded.
- [ ] Each interaction has a unique ID.
- [ ] Date and time are present for every interaction.
- [ ] The author is identified as either `couple` or one student matricola.
- [ ] The full prompt is included.
- [ ] The full LLM response is included or correctly referenced as a separate file.
- [ ] Every downloaded/generated file has a unique name, is present in the repository and is correctly referenced in this log.
- [ ] Problems, errors, and hallucinations have been reported honestly.
