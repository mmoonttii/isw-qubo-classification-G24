Let's optimize `feature_selection.py`. I'll target the three real bottlenecks for a 1.5M-row training set: the loading pattern (which currently duplicates memory), the O(n²) simulated-annealing fallback (whose cost scales with how many alpha-iterations a large/awkward dataset needs), and give you an opt-in way to cap correlation-computation cost on very large row counts without touching what gets written to the output CSVs.Now the core loading section:Now the correlation section, adding optional subsampling and float32 casting:Now I'll replace it with an O(n)-per-step incremental version:Now let's add the CLI flag and wire it through:Let me verify the rewrite is actually correct before handing it back — I'll test it against a synthetic dataset and cross-check the delta-based solver against a brute-force recomputation.The math checks out to floating-point precision. Now an end-to-end run on synthetic data:Let's benchmark the actual speedup at the project's real scale (n≈145 features, since that's what dominates SA runtime) against the original O(n²) approach:The theoretical complexity win (O(n) vs O(n²)) is real, but let me get an honest empirical number at the full step count rather than trusting extrapolation — small-n BLAS calls are already fast, so I want to know what the constant-factor gains actually look like before I claim a number to you.Good — honest number in hand. Let me correct the docstrings I wrote (I'd initially guessed at a much bigger speedup than what's actually measured — worth fixing before handing this back):Let me also verify the memory-savings claim empirically rather than just asserting it:Also more modest than I originally guessed — let me fix that claim too:Now let's re-verify the full pipeline still works after these edits, and confirm the new `--max-corr-samples` option functions correctly on the larger dataset:Matches the spec's required schema exactly, unchanged. Let's do a final full read-through, then hand back the file.This all checks out. Let's copy it to outputs and hand it back.Three changes, all verified before I handed them back — not just asserted:

**1. Loading (the memory spike fix).** The old code read the file in 128k-row chunks and then `pd.concat()`-ed them — that pattern doesn't save memory, since the chunk list and the final frame coexist briefly, and it still used pandas' default float64/int64. I replaced it with a single dtype-typed read (float32 features, int8 target). Measured on a synthetic 300k×145 dataset: **peak RSS 745 MB → 318 MB** (~2.3x), which scales to roughly **3.7 GB → 1.6 GB** at 1.5M rows.

**2. The QUBO fallback solver (`_optimize_qubo_numpy`).** This is the pure-numpy simulated annealing used when `dwave-neal` isn't installed. It used to recompute `x^T Q x` from scratch — O(n²) — after every proposed bit flip. I replaced that with an incremental O(n) update, derived from Q's symmetry, and verified the delta formula against brute-force recomputation at every single step (not just spot-checked) across 20 random matrices — max error `7×10⁻¹⁴`, i.e. exact up to floating point. Real speedup at your project's feature count (n≈145): **~2.8x per restart**. I initially guessed a much bigger number in the docstring based on the theoretical complexity gap, benchmarked it, found I was wrong, and corrected the comment — worth knowing since at n≈145 the O(n²) recompute was already a single BLAS call, so most of the win is from cutting per-step Python overhead, not raw FLOPs.

**3. Optional correlation subsampling** (`max_corr_samples`, default `None`). Ranking 1M+ rows for Spearman correlation is the one step whose cost scales directly with your row count. This lets you cap how many training rows feed *just* the correlation estimate — statistically sound since a rank correlation's standard error shrinks as 1/√sample size. It's opt-in and only touches the correlation step; I confirmed the saved `reducedTrain_csv`/`reducedTest_csv` still contain the **full, unsampled** split (210,000 and 90,000 rows respectively in my test) regardless of the setting, so it doesn't violate the spec's requirement to use the actual training set for the reduced datasets.

I also confirmed the output JSON still matches the spec's exact required schema (no fields added or removed), and reran the whole thing end-to-end afterward — identical feature selection, alpha, and cost values as before the changes, on the same seed.

One thing I did *not* touch: `select_features` still loads the full (now smaller) normalized dataset into one DataFrame, because Spearman correlation genuinely needs to see whole columns at once — that's not fixable without a fundamentally different, much more complex algorithm (e.g. Dask/out-of-core rank computation). If you ever hit real memory limits at the full 1.5M-row scale, `max_corr_samples` is the lever to pull rather than more engineering here.