Excellent diagnosis material — both bugs are real and have distinct root causes. Let me trace through Streamlit's execution model to confirm before fixing.Confirmed both root causes:

**Bug B (the "everything resets" one):** `st.file_uploader` keeps returning the *same* `UploadedFile` object on every script rerun until the widget value actually changes — and in Streamlit, every button click reruns the entire script top to bottom. So `if uploaded is not None: _handle_upload(uploaded)` fires again on *every single interaction*, including clicking "Start Feature Selection," and `_handle_upload()` unconditionally wipes `preprocessing_done`, `feature_selection_done`, etc. back to `False` every time it runs. That's why Phase 2 runs successfully in the console (the call itself isn't broken) but the moment the script reruns to show the result, the upload handler resets the state out from under it.

**Bug A (the stale sidebar):** the sidebar is rendered *before* the tab content in `main()`. When you click "Start Preprocessing," Streamlit reruns the whole script once: the sidebar paints with the *old* `preprocessing_done` value first, then `_run_preprocessing()` updates state, then the tabs render and correctly see the new value (which is why the gate opens) — but the sidebar snapshot was already taken and won't refresh until another rerun happens.

Let's fix both. First, the upload guard:Now register the new tracking key in `_STATE_DEFAULTS` so "Reset Pipeline" clears it too, and so a re-upload of the *same* dataset after a reset is correctly detected as new:Now let's fix Bug A — adding `st.rerun()` after each phase successfully completes, so the sidebar re-renders with fresh state instead of the stale snapshot taken before the action ran.Now the same fix for Training and Prediction, for consistency: