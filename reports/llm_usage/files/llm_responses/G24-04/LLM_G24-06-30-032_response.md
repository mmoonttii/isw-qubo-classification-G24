Good catch — I see exactly what's broken. Two of the dual-theme CSS tokens I set up earlier got bypassed: the page header uses hardcoded inline colors instead of the theme classes, the status badges assume "the accent color is always light enough for black text" (false in light mode), and the Plotly chart is hardcoded to `plotly_dark` regardless of theme. Let me fix all three.**Bug 1: badge contrast.** `--accent` is bright teal in dark mode (so black text works) but a *darkened* teal in light mode for text-on-white contrast — used as a badge background, that same dark teal needs white text, not black. The CSS comment even says "teal is always light," which is only true for the dark-mode value.Now wire up the two badges that used hardcoded `#000`:**Bug 2: the page header.** It bypasses the theme system entirely with inline `style="color:#E6EDF3"` — that's the dark-mode text color hardcoded, so in light mode it's near-invisible light-grey text on a white page. There are already correct `.page-title`/`.page-subtitle` classes sitting unused in the CSS — I just need to use them.**Bug 3: the alpha-search chart.** It's hardcoded to `template="plotly_dark"` with dark navy/grey colors, so in light mode it renders as a jarring dark box sitting in an otherwise white page, with low-contrast text. Let me make it theme-aware, mirroring the same palette already defined in the CSS tokens.Now let's verify everything's syntactically valid and double-check no hardcoded theme-locked colors remain:All clean — the only remaining hex values are inside the new `_CHART_PALETTES` dict, which is the intended theme-aware lookup table. Line 163 is just a code comment.Three bugs were causing this, all from places that bypassed the dual-theme token system instead of using it:

1. **Page title/subtitle** — hardcoded `color:#E6EDF3` inline on the `<h1>`, which is the dark-mode text color. On a white background that's near-invisible. Fixed by routing through the `.page-title`/`.page-subtitle` CSS classes that already correctly reference `var(--text-primary)`.

2. **Status badges** (`.phase-number`, active pipeline step) — text was hardcoded to `color:#000` with a comment assuming "teal is always light." True for the dark-mode accent (`#00C9A7`, bright), false for light mode's accent (`#0A8A74`, a deliberately *darkened* teal meant for text-on-white). Black text on that dark teal had poor contrast. Added a `--accent-contrast` token (`#000` dark / `#fff` light) so the badge label always reads clearly.

3. **Alpha-search chart** — Plotly was hardcoded to `template="plotly_dark"` with GitHub-dark hex values, so in light mode it rendered as a dark navy box dropped into an otherwise white page. Plotly can't read the browser's `prefers-color-scheme`, so I added theme detection via `st.context.theme` (falling back to `st.get_option("theme.base")`, then `"dark"`) and a `_CHART_PALETTES` dict that mirrors the exact same hex values already used in the CSS tokens — so the chart now matches the page in either mode instead of fighting it.