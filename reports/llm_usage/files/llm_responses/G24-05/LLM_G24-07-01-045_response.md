Makes sense — mirrors the `outputs/` pattern for symmetry. I'll add a `_resolve_data_dir()` + `_resolve_input_path()` pair (same "try literal path, then fall back to `data/<basename>`" logic used for the other two files), and use it for `--input`.Now wire it into `fit_normalize()`, where the input file is opened:Now let's re-run the smoke test from repo root, using a bare filename (`input_dataset.csv`) that only exists in `data/`, exactly as the spec's example command does:Works — `--input input_dataset.csv` (bare filename, not present in the cwd) resolved to `data/input_dataset.csv` automatically. Let's also confirm a full/relative path still works, since that's the fallback-first branch:Both forms work. Copying the updated file to your outputs:`preprocessing.py` now mirrors the other two modules: `--input` is resolved by trying the path exactly as given first, then falling back to `data/<basename>` — so bare filenames like `input_dataset.csv` or `sample_test_dataset.csv` resolve automatically to `data/` regardless of cwd, while an explicit path (`data/input_dataset.csv`, or an external dataset with a full path) still works unchanged.