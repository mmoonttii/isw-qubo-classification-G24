Let's start with `model.py` — I already know exactly which lines to change.Now `preprocessing.py` — same fix, plus the CLI needs to pre-resolve since it currently relies on the internal remap.Good. Now fix the CLI block to pre-resolve before calling `fit_normalize()`:Now `feature_selection.py` — same pattern:Good. Now fix `feature_selection.py`'s CLI to pre-resolve before calling `select_features()`:All three modules are consistent now — `_to_output_path()` only fires at CLI dispatch boundaries. Now `gui.py`:Now the `train()` and `predict()` call sites in `gui.py`:Now `test_pipeline.py`'s `preproc`/`feat_sel` fixtures — these need to pass fully-resolved paths instead of relying on the (now-removed) internal remap.Now update these to pass fully-resolved paths, since `fit_normalize()`/`select_features()` honor whatever they're given literally:Good, those two fixtures already match the new (fixed) contract. Now let's do a final syntax/consistency check across all four edited files.Let's actually exercise the fixed functions end-to-end against the static dataset to confirm this really works, not just parses.Both available. Let's do a real end-to-end run using `tmp`-style absolute paths (mimicking exactly what the evaluator/pytest does), not just `outputs/`.All four functions now write to exactly the caller-given path — including `train()`, which reproduces (and confirms the fix for) the exact failure from your test log. Now let's confirm the CLI path still works too, since that's the other half of the contract:Both call paths work correctly: bare CLI filenames still resolve into `outputs/`, and explicit paths (as used by tests/evaluators) are honored literally. Let's also test `feature_selection.py`'s and `model.py`'s CLI to be thorough, then package everything for delivery.All four CLI commands still work end-to-end, and the full pipeline runs correctly through to predictions. Everything's verified. Packaging the deliverables now.All five files are fixed and verified with actual runs (not just review):

- **`preprocessing.py`** — `fit_normalize()` honors `normalized_csv`/`outInitalRes_json` literally; CLI now pre-resolves bare filenames before calling it.
- **`feature_selection.py`** — `select_features()` honors all four output params literally; CLI pre-resolves the same way.
- **`model.py`** — `train()`/`predict()` honor their output params literally (this was your original bug); CLI already did the right thing and is unchanged.
- **`gui.py`** — all four call sites (`fit_normalize`, `select_features`, `train`, `predict`) now pass `str(_outputs_dir() / "filename")` explicitly instead of relying on the backend to redirect bare filenames.
- **`test_pipeline.py`** — the `preproc`/`feat_sel` fixtures now pass fully-resolved paths instead of the bare-filename workaround, so they actually exercise the "honor the literal path" contract like `model_outputs`/`prediction_outputs` already did.

I verified this two ways in the sandbox: ran the whole pipeline (`fit_normalize → select_features → train → predict`) with paths in an external `/tmp` directory outside the repo and confirmed every output landed exactly where specified — including reproducing and fixing the precise `train()` failure from your log — and separately ran all four CLI commands with bare filenames to confirm they still land correctly in `outputs/`.

Drop these five files into your repo and re-run `pytest` — the 3 failures should now pass, and the classifier-skip results (for names not yet implemented) are unaffected since that was a separate, expected outcome.