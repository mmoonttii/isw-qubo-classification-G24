This confirms what I suspected — the bug isn't the *values* of the tokens, it's the *trigger* mechanism. I used `@media (prefers-color-scheme: light)`, which only reacts to the OS/browser preference. But Streamlit has its own independent theme switcher (☰ menu → Settings → Theme), and when someone picks "Light" there while their OS is set to dark — or the hosting environment forces a theme — the CSS media query never fires. Result: the page background goes light (controlled by Streamlit itself) while my custom components stay locked to dark-mode tokens, which is exactly the ghost text you're seeing.

The fix is to stop guessing from the OS and instead ask Streamlit directly what theme it's actually rendering, then emit only the matching token set — no media query needed.Now I have exact boundaries (lines 62–374). Let me restructure this into a Python-driven theme system: a token dictionary, theme detection (moved up and reused everywhere), and a CSS builder that injects only the detected theme's values — no media query involved.Now replacing the entire token section (lines 57–169, the `:root` + `@media` block) with a Python-driven equivalent:Now find and remove the now-duplicate `_detect_theme_base()` I'd previously placed near the chart code, and update the CSS injection call in `main()`:Now update `main()` to detect the theme once and inject the generated root block alongside the static CSS body:Good — `_build_root_css()` opens `<style>`, and the static `_CSS` body still closes it at line 377. The concatenation `_build_root_css(theme) + _CSS` produces one well-formed `<style>...</style>` block. Now let's validate everything:Now let's actually render the generated CSS for both themes and sanity-check the output is well-formed:The theme detection and CSS generation work correctly — when Streamlit reports `light`, the generator correctly produces dark-on-white tokens (`--text-muted: #656D76`, `--accent-contrast: #fff`), not the previous media-query-only defaults.Both paths resolve correctly, and the dark/light token dictionaries are symmetric (no missing variables that would fall back to `unset`/`inherit` in either mode).The screenshot pinpointed it exactly: notice the page title and subtitle are perfectly readable (dark text on white), while the sidebar logo, the "Preprocessing" phase heading, the unselected tab labels, and the pending pipeline steps are all ghost-faint. That split is the tell — those readable elements use the `.page-title`/`.page-subtitle` classes I fixed last time, while everything still washed out uses `var(--text-muted)`, `var(--tab-color)`, and `var(--ps-pending-clr)`, which were only ever being overridden by `@media (prefers-color-scheme: light)`.

That media query reacts to the OS/browser setting, not to Streamlit's own theme switcher. If your OS prefers dark but Streamlit's in-app theme (or the hosting environment) is forcing light, Streamlit paints the page background white while my CSS never got the signal to switch — so it stayed on the dark-mode token values (near-white text at low opacity), invisible against the now-white page.

The fix replaces the CSS media query with direct detection of what Streamlit is actually rendering, via `st.context.theme.type` (falling back to `st.get_option("theme.base")`, then `"dark"`). All 22 color tokens now live in a Python dict keyed by `"dark"`/`"light"`, and a `_build_root_css()` function emits only the matching `:root {...}` block at render time — deterministic, and impossible for the OS and the app to disagree on. The same detection function is reused for the Plotly chart, so everything stays in sync from one source of truth.