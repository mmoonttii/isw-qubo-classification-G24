I found the bug. Let me confirm the structure once more, then fix it.Found it. Here's what's happening:

**The bug is in `model.py`.** Its `train()` and `predict()` functions write their output files (`model.joblib`, `training_metrics.json`, `predictions.csv`, `classification_stats.json`) using whatever path string is passed in, with **no resolution to the `outputs/` directory** — they just do `joblib.dump(model, model_path)` / `df.to_csv(predictions_csv)` directly.

`preprocessing.py` and `feature_selection.py` both guard against this with a `_resolve_outputs_dir()` / `_to_output_path()` helper that always resolves a bare filename to the real `outputs/` folder (found by walking up from `__file__`), *regardless of the process's current working directory*. `model.py` has the exact same helper functions — but they're defined **only inside `if __name__ == "__main__":`**, so they're only used by the CLI. The library functions `train()` / `predict()` (which is what `gui.py` calls directly) never go through them.

`gui.py` calls `train(model_path="model.joblib", metrics_json="training_metrics.json", ...)` with bare filenames, so the model actually gets written to `Path.cwd()/model.joblib` — wherever Streamlit happens to be launched from (typically the repo root, not `outputs/`). Meanwhile `gui.py` then sets `st.session_state.model_path = str(_outputs_dir() / "model.joblib")`, assuming it was written there. If `outputs/model.joblib` already exists from any earlier run (a prior CLI test, an earlier Phase‑2 run with different `percSelected`/`allowance`, a checked-in sample artifact, etc.), the Prediction phase silently loads that **stale** model — trained on a different selected-feature set than the brand-new `reduced_test_csv` — which is exactly the "feature names unseen at fit time / now missing" error you're seeing. Same issue affects `predictions.csv` and `classification_stats.json`.

The fix is to make `model.py`'s `train()`/`predict()` resolve their own output paths internally, the same way the other two modules do, so it's correct no matter who calls it (CLI or GUI).Now wire the helper into `train()`:Good. Now `predict()`:These local duplicates inside `__main__` are now redundant (and shadow the module-level ones harmlessly, but they're dead weight). I'll remove them since the CLI block already calls `_to_output_path(...)`, which now resolves to the module-level version.Now let's sanity-check the whole file compiles and the logic reads correctly end to end:Let's verify this actually reproduces and fixes the issue with a quick simulation:Confirmed — exact reproduction of the reported error. Now let's verify the fix eliminates it:The fix works cleanly — `model.joblib` is now correctly written to (and overwrites) `outputs/`, no stray copy is left in the CWD, and prediction succeeds with the freshly trained model. Let me save the fixed file for you.**Root cause:** `model.py`'s `train()` and `predict()` write their outputs (`model.joblib`, `training_metrics.json`, `predictions.csv`, `classification_stats.json`) to whatever path string they're given, with no resolution to the project's `outputs/` directory. `preprocessing.py` and `feature_selection.py` both guard against this with a `_resolve_outputs_dir()`/`_to_output_path()` helper — `model.py` has the identical helper, but it was defined only inside `if __name__ == "__main__":`, so it only protects the CLI path, never the library functions that `gui.py` actually calls.

`gui.py` passes bare filenames like `model_path="model.joblib"`, so the model lands in `Path.cwd()` (wherever Streamlit was launched from) instead of `outputs/`. Meanwhile `gui.py` sets `session_state.model_path` to `outputs/model.joblib`, assuming that's where it landed. If `outputs/model.joblib` already exists from any earlier run — a prior CLI test, an earlier Phase‑2 iteration with a different `percSelected`/`allowance`, etc. — Phase 4 silently loads that stale model, trained on a different feature set than the brand-new `reduced_test_csv`. That's exactly the "unseen at fit time / now missing" error.

**Fix:** moved `_resolve_outputs_dir()`/`_to_output_path()` to module level in `model.py` and applied them to `model_path`/`metrics_json` inside `train()` and to `predictions_csv`/`classif_stats_json` inside `predict()`, so these always resolve into the real `outputs/` directory regardless of caller or CWD — exactly matching how `preprocessing.py`/`feature_selection.py` already behave. I verified this reproduces your exact error with the original file and confirms it's gone with the fix above. No changes to `gui.py` or `feature_selection.py` were needed.